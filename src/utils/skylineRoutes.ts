/**
 * Skyline 自定义路由
 * 页面转场已改用内置预设路由 + ShareElement 共享元素动画
 * 内置路由类型：
 *   wx://bottom-sheet      半屏弹窗
 *   wx://cupertino-modal   iOS 风格模态（评论页）
 *   wx://modal             模态弹窗
 *   wx://zoom              放大进入
 */

export function registerSkylineRoutes() {
  // 自定义路由已移除，使用内置预设路由
}
